using UnityEngine;

[RequireComponent(typeof(StartMenu))]
public class GameStart : MonoBehaviour
{
    void Awake()
    {
        var menu = GetComponent<StartMenu>();
        menu.StartGame();
    }
}
